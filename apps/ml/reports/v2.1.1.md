# Báo Cáo Cập Nhật Sửa Đổi & Kiểm Định Notebook 2.0 (Phiên Bản v2.1.1)

Tài liệu này ghi nhận việc khắc phục 2 vấn đề tồn đọng trong file [apps/ml/notebooks/2.0-global-classification-poc.ipynb](file:///Users/huongnguyen/Desktop/Học%20Python/Project/Machine%20Learning/machine_learning_projects/stockTrendPrediction/apps/ml/notebooks/2.0-global-classification-poc.ipynb):

---

## 1. Các Vấn Đề Đã Sửa Đổi Trực Tiếp

### 🔧 [Vấn đề A] Bổ sung import `timedelta` cho nhánh gọi API VNINDEX
- **Hiện tượng trước khi sửa**: Trong Cell 4 ("BƯỚC 1: THU THẬP DỮ LIỆU ĐA MÃ"), nhánh gọi API sống của VNINDEX sử dụng `timedelta(days=chunk_days)` nhưng Cell 2 (imports) chỉ có `from datetime import datetime`. Nếu không có cache, notebook sẽ lỗi `NameError: name 'timedelta' is not defined`.
- **Thực hiện sửa**:
  - Tại Cell 2: Đổi thành:
    ```python
    # 🔧 [SỬA ĐỔI v2.1.1 — Vấn đề A] Bổ sung import timedelta từ datetime cho nhánh gọi API VNINDEX
    from datetime import datetime, timedelta
    ```
  - Tại Cell 4: Bổ sung chú thích rõ ràng tại vị trí dùng `timedelta`.
- **Kiểm định thực tế nhánh gọi API sống**:
  - Tạm thời backup file [VNINDEX.csv](file:///Users/huongnguyen/Desktop/Học%20Python/Project/Machine%20Learning/machine_learning_projects/stockTrendPrediction/apps/ml/data/raw/VNINDEX.csv) sang `.backup_test`.
  - Chạy thực tế khối lệnh gọi API SSI FastConnect `/api/v2/Market/DailyIndex`.
  - **Kết quả**: Thu thập thành công **917 phiên giao dịch** trong **72.69 giây** không phát sinh bất kỳ lỗi nào.
  - Phục hồi lại file cache gốc an toàn, bảo toàn 100% dữ liệu.

### 🔧 [Vấn đề B] Cập nhật mô tả Macro F1 của LSTM trong bảng lý thuyết
- **Hiện tượng trước khi sửa**: Trong Cell 9 ("CƠ SỞ LÝ THUYẾT & PHÂN TÍCH LỰA CHỌN MÔ HÌNH HỌC MÁY"), dòng mô tả PyTorch LSTM trong bảng ưu/nhược điểm vẫn ghi con số cũ: `"đạt Macro F1 cao nhất (0.3792)"`.
- **Thực hiện sửa**:
  - Cập nhật câu mô tả thành:
    ```markdown
    <!-- 🔧 [SỬA ĐỔI v2.1.1 — Vấn đề B] Cập nhật kết quả Macro F1 của LSTM đồng bộ với kiểm định Walk-Forward v2.1 -->
    • Phân bổ dự đoán đều cả 3 lớp Tăng/Giảm/Đi ngang, đạt **Mean Macro F1 cao nhất (0.3384 ± 0.0074 qua 3 cửa sổ Walk-Forward, cực kỳ ổn định)**.
    ```

---

## 2. Kết Quả Chạy Lại Toàn Bộ Notebook

Toàn bộ 15 cell đã được chạy tự động lại từ đầu đến cuối trong **73.56 giây**:
- **Tổng số lỗi (code errors)**: **0**.
- **Kết quả số liệu qua 3 cửa sổ Walk-Forward**: 100% giữ nguyên tính nhất quán, không thay đổi:
  - **Naive Baseline**: Mean Macro F1 = **0.2054 ± 0.0150** (W1: 0.1955, W2: 0.1942, W3: 0.2266)
  - **LightGBM**: Mean Macro F1 = **0.2746 ± 0.0355** (W1: 0.2246, W2: 0.3033, W3: 0.2959) — *vượt Naive ở cả 3 cửa sổ (+33.7%)*
  - **PyTorch LSTM**: Mean Macro F1 = **0.3384 ± 0.0074** (W1: 0.3454, W2: 0.3417, W3: 0.3282) — *vượt trội nhất (+64.7%), cực kỳ ổn định*
